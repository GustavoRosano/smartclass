import React from "react";
import styles from './styles.module.scss';

export default function Card({ classNumber, teacher, title, image }: any) {

  return (
    <div className={styles.card}>Card</div>
  );
}